<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mikel.gestordepartidos">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/vmain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Login"
            android:label="@string/vlogin" />
        <activity
            android:name=".ListadoInvitado"
            android:label="@string/vlista" />
        <activity android:name=".mostrar_partido" />
        <activity android:name=".ListadoUsuario" />
        <activity android:name=".EditarPartidoUsuario" />
        <activity android:name=".ListadoCoordinador" />
        <activity android:name=".EditarPartidoCoordinador"></activity>
    </application>

</manifest>